import { DateOfCreationPipe } from './date-of-creation.pipe';

describe('DateOfCreationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOfCreationPipe();
    expect(pipe).toBeTruthy();
  });
});
